["advice", "disclosure", "resolution", "closing", "objection_handling"]
        
🟩 Setup Phase 
Policies (fake)
    ↓
Chunk "if necessary" 
    ↓
Embeddings (sentence-transformers: all-MiniLM-L6-v2 — local)
    ↓
Store in FAISS (policy_index as vectors)
---------------------------------------------
🟦 Runtime Phase (per transcript)
Transcript (fake)
    ↓ For each utterance:
        ↓ Embed (sentence-transformers: all-MiniLM-L6-v2 — local)
        ↓ Query FAISS → get top-k (e.g., k=3) policy indices
        ↓ Convert indices → original policy TEXT	
        ↓ If similarity < threshold:
                mark as "low confidence"
        ↓ Send to LLM (Groq API "Llama 4 Scout"):
                - Utterance
                - Retrieved policies
        ↓ Output:
                - Violation (Yes/No)
                - Violated Policy
                - Matched Policy ID / Name
                - Reason
                - Evidence
                - Confidence:
                - High → similarity ≥ threshold
                - Low → similarity < threshold